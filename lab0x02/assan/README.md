# Assan

You can overwrite the characters of the `ls` command in `command` buffer by jumping above the red-zone with the adequate second parameter as program argument. Replace it with `sh` for example.
